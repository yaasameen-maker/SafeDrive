import React from 'react';
import ReactDOM from 'react-dom/client';
import SafeDriveApp from './SafeDriveApp';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <SafeDriveApp />
  </React.StrictMode>,
)
